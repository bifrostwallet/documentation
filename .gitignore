# Dependencies & build outputs
/node_modules
/build
/dist
/out
.docusaurus
.cache-loader
*.log

# Yarn (zero-install artifacts are not committed; the release binary and its
# hash file are, so installs use the reviewed, hash-verified binary)
.yarn/*
!.yarn/cache
!.yarn/yarn-binary-hash.txt
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# --- OS ---
.DS_Store
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini
*~

# --- Editors / IDEs (local only; not part of the public project) ---
.idea/
.fleet/
.vscode/
.cursor/
.history/
.ionide/
.vim/
.netrwhist
*.swp
*.swo
*.swn
*.sublime-project
*.sublime-workspace
*.code-workspace
*.tmproj
*.tmproject
*.iml
*.ipr
*.iws
.project
.classpath
.settings/
*.launch
.emacs.desktop
.emacs.desktop.lock
*_flymake.*
\#*\#
.\#*
tags
TAGS
cscope.out

# Env
.env
.env.*
!.env.example

# Claude Code — keep project settings out of the tree; .claude/settings.json
# is a known SessionStart persistence surface in Shai-Hulud-style npm worms.
.claude/

# Cursor project hooks — sessionStart-style autostart (rules/commands/mcp stay allowed).
.cursor/hooks.json
.cursor/hooks/

# Prefer .git-hooks/; common git-hook persistence drop.
.husky/
